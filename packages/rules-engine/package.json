{
    "name": "@lead-engine/rules-engine",
    "version": "1.0.0",
    "private": true,
    "description": "Canonical 7-gate buyer-rule evaluation + field filters + StrategySpec. Single source of truth consumed by the CRE workflow (DON), the backend auto-bid engine, and cre.service.",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist",
        "src"
    ],
    "scripts": {
        "build": "tsc",
        "prepare": "tsc",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "zod": "^3.22.0"
    },
    "devDependencies": {
        "fast-check": "^3.15.0",
        "typescript": "^5.3.2",
        "vitest": "^1.2.0"
    }
}
