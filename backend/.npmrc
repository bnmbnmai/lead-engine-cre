# Ensure devDependencies are always installed in all environments.
# Render (and other CI hosts) set NODE_ENV=production which causes npm to
# skip devDependencies. We need them for `tsc` and @types/* during the build.
production=false
